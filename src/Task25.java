class ГорячийНапиток {
    private String имя;
    private int объем;
    private int температура;

    public ГорячийНапиток(String имя, int объем, int температура) {
        this.имя = имя;
        this.объем = объем;
        this.температура = температура;
    }

    public String получитьИмя() {
        return имя;
    }

    public int получитьОбъем() {
        return объем;
    }

    public int получитьТемпературу() {
        return температура;
    }
}

// Наследник класса ГорячийНапиток с дополнительным полем температура
class ГорячийНапитокСТемпературой extends ГорячийНапиток {
    private int температура;

    public ГорячийНапитокСТемпературой(String имя, int объем, int температура) {
        super(имя, объем, температура);
        this.температура = температура;
    }

    public int получитьТемпературу() {
        return температура;
    }
}

// Класс ТорговыйАвтомат
class ТорговыйАвтомат {
    public ГорячийНапиток getProduct(String имя, int объем) {
        // Здесь можно добавить логику по получению продукта по имени и объему
        return null;
    }
}

// Наследник класса ТорговыйАвтомат, реализующий ГорячихНапитков
class ГорячихНапитковАвтомат extends ТорговыйАвтомат {
    public ГорячийНапиток getProduct(String имя, int объем, int температура) {
        // Здесь можно добавить логику по получению горячего напитка с учетом температуры
        // Например, можно искать напиток в базе данных по имени, объему и температуре
        return new ГорячийНапитокСТемпературой(имя, объем, температура);
    }
}

public class Task25 {
    public static void main(String[] args) {
        ГорячихНапитковАвтомат автомат = new ГорячихНапитковАвтомат();

        ГорячийНапиток горячийНапиток1 = автомат.getProduct("Чай", 250, 85);
        ГорячийНапиток горячийНапиток2 = автомат.getProduct("Кофе", 350, 95);

        System.out.println("Продукт 1: " + горячийНапиток1.получитьИмя() + ", Объем: " + горячийНапиток1.получитьОбъем() + ", Температура: " + горячийНапиток1.получитьТемпературу());
        System.out.println("Продукт 2: " + горячийНапиток2.получитьИмя() + ", Объем: " + горячийНапиток2.получитьОбъем() + ", Температура: " + горячийНапиток2.получитьТемпературу());
    }
}